# Punnett
Punnet square generator.

## Use
1. Install Java 8 or higher if you don't have it
2. Download the latest version from [here](https://ci.pantherman594.com/job/Punnett/lastSuccessfulBuild/).
3. Run the Punnett.jar file from terminal/command prompt using the command `java -jar Punnett.jar`.
4. Enter the parent genotypes and press enter on your keyboard.